// Renderer 5.0 floating-point scene targets.
static void renderer500_delete_targets(void){if(g_fbo500&&p500DeleteFramebuffers)p500DeleteFramebuffers(1,&g_fbo500);g_fbo500=0;if(g_color500)glDeleteTextures(1,&g_color500);if(g_depth500)glDeleteTextures(1,&g_depth500);g_color500=g_depth500=0;g_renderer500_w=g_renderer500_h=0;g_renderer500_hdr=0;}
static int renderer500_create_targets(int w,int h){
 if(!g_renderer500_ready||w<64||h<64)return 0;renderer500_delete_targets();g_renderer500_w=w;g_renderer500_h=h;
 glGenTextures(1,&g_color500);glBindTexture(GL_TEXTURE_2D,g_color500);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MIN_FILTER,GL_LINEAR);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MAG_FILTER,GL_LINEAR);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_S,GL_CLAMP_TO_EDGE);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_T,GL_CLAMP_TO_EDGE);glTexImage2D(GL_TEXTURE_2D,0,GL_RGBA16F,w,h,0,GL_RGBA,GL_FLOAT,NULL);
 glGenTextures(1,&g_depth500);glBindTexture(GL_TEXTURE_2D,g_depth500);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MIN_FILTER,GL_NEAREST);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_MAG_FILTER,GL_NEAREST);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_S,GL_CLAMP_TO_EDGE);glTexParameteri(GL_TEXTURE_2D,GL_TEXTURE_WRAP_T,GL_CLAMP_TO_EDGE);glTexImage2D(GL_TEXTURE_2D,0,GL_DEPTH_COMPONENT24,w,h,0,GL_DEPTH_COMPONENT,GL_UNSIGNED_INT,NULL);
 p500GenFramebuffers(1,&g_fbo500);p500BindFramebuffer(GL_FRAMEBUFFER,g_fbo500);p500FramebufferTexture2D(GL_FRAMEBUFFER,GL_COLOR_ATTACHMENT0,GL_TEXTURE_2D,g_color500,0);p500FramebufferTexture2D(GL_FRAMEBUFFER,GL_DEPTH_ATTACHMENT,GL_TEXTURE_2D,g_depth500,0);u32 s=p500CheckFramebufferStatus(GL_FRAMEBUFFER);p500BindFramebuffer(GL_FRAMEBUFFER,0);glBindTexture(GL_TEXTURE_2D,0);if(s!=GL_FRAMEBUFFER_COMPLETE){renderer500_delete_targets();return 0;}g_renderer500_hdr=1;return 1;
}
