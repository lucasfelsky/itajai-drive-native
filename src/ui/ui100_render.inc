// -----------------------------------------------------------------------------
// Production help/system overlay. Version text follows the centralized release ID.
// -----------------------------------------------------------------------------
static void ui100_status_line(float x,float y,const char*name,int ok){char b[104];sprintf(b,"%-19s %s",name,ok?"OK":"...");gl_text(x,y,b,ok?.64f:.92f,ok?.92f:.72f,ok?.70f:.38f);}
static int ui200_vehicle_asset_count(void){vehicle160_resolve();int n=0;for(int i=0;i<CAR160_COUNT;i++)if(g_car_asset160[i]>=0)n++;return n;}
static void ui100_draw_help(void){
  if(!g_help100||g_photo70_hide_hud)return;float w=730,h=410,x=28,y=(float)g_h-h-28;if(y<62)y=62;hud_panel(x,y,w,h);
  gl_text(x+20,y+28,"ITAJAI DRIVE " ITAJAI_VERSION " // CONTROLES + SELF-CHECK",1,.94f,.76f);
  gl_text(x+20,y+55,"W/S          acelerar / frear / re",.86f,.90f,.94f);gl_text(x+20,y+77,"A/D          direcao (intuitiva em re)",.86f,.90f,.94f);gl_text(x+20,y+99,"SPACE        freio de mao",.86f,.90f,.94f);gl_text(x+20,y+121,"C / E        camera / trocar carro",.86f,.90f,.94f);gl_text(x+20,y+143,"R / M        reset / abrir mapa + GPS",.86f,.90f,.94f);gl_text(x+20,y+165,"P / H        Photo Mode / esconder HUD",.86f,.90f,.94f);gl_text(x+20,y+187,"F2 / F3      Drive Hub / garagem",.86f,.90f,.94f);gl_text(x+20,y+209,"SHIFT+F2/F10 tour / editor de mundo",.86f,.90f,.94f);gl_text(x+20,y+231,"TAB T Y      debug / horario / clima",.86f,.90f,.94f);gl_text(x+20,y+253,"F1 / ESC     ajuda / menu",.86f,.90f,.94f);
  float sx=x+382,sy=y+55;gl_text(sx,sy,"SISTEMAS",.70f,.86f,1.0f);int cars=ui200_vehicle_asset_count();ui100_status_line(sx,sy+25,"OSM / estradas",g_world.roadCount>0);ui100_status_line(sx,sy+43,"grafo / A*",g_world.edgeCount>0);ui100_status_line(sx,sy+61,"colisoes",g_static_collider_count>0);ui100_status_line(sx,sy+79,"asset PAK",g_asset_pack_ok08);ui100_status_line(sx,sy+97,"PAK 32 assets",g_asset_count08>=32);ui100_status_line(sx,sy+115,"frota 8/8",cars==CAR160_COUNT);ui100_status_line(sx,sy+133,"OpenGL context",g_glrc!=NULL);ui100_status_line(sx,sy+151,"renderer core",g_renderer09_init);ui100_status_line(sx,sy+169,"audio",g_audio_started18);ui100_status_line(sx,sy+187,"editor",g_landmark_count19>0);ui100_status_line(sx,sy+205,"mundo vivo",g_life50_init);ui100_status_line(sx,sy+223,"streaming",g_stream_ready08);ui100_status_line(sx,sy+241,"road stream",g_stream_road_count08>0);ui100_status_line(sx,sy+259,"footprints",g_building_polys12>0);
  int visual=(g_asset_count08>=32)+(cars==CAR160_COUNT)+(g_renderer09_init?1:0)+(g_stream_road_count08>0);int total=g_selfcheck100+visual;char b[240];sprintf(b,"SELF-CHECK %d/14  |  GLSL %s  |  TOUR: %s",total,g_renderer09_shader?"ATIVO":"FALLBACK",g_tour100_name);gl_text(x+20,y+326,b,total==14?.72f:1.0f,total==14?.94f:.72f,total==14?.76f:.38f);gl_text(x+20,y+350,"M abre o mapa: clique/ENTER define destino; roda +/- zoom; DEL cancela rota.",.62f,.70f,.78f);gl_text(x+20,y+374,"Fallbacks mantem o jogo aberto; TAB mostra telemetria detalhada de cada subsistema.",.58f,.65f,.72f);
}
